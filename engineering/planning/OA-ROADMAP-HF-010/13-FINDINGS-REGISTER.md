# Findings Register

Status: `INDEPENDENT EVIDENCE REGISTER — NON-AUTHORITATIVE`

| ID / class | Finding and evidence | Affected architecture / impact | Recommendation | Verification criterion |
|---|---|---|---|---|
| F-001 Blocking | No canonical metadata store or schema registry is selected/adopted. Evidence: HF-009 `10` lists both as prerequisites; HF-009 `12` lists concrete schema/persistence as a gap. | EMM, all consumers; authoritative uniqueness and durable revision resolution cannot be implemented. | Define the logical registry/store interface and its owner/immutability guarantees. | Fixtures prove unique resolution, immutable successors, and retained lineage. |
| F-002 Blocking | Interface payload and error contracts are absent. Evidence: HF-009 `12` explicitly identifies them as missing. | EMP, Zeus, EOS, EENS, API, generator; integrations cannot interoperate predictably. | Specify versioned logical request/response, error, and compatibility contracts. | Cross-subsystem fixtures accept/reject the same version/mismatch cases. |
| F-003 Blocking | No machine-resolvable owner directory/delegation model exists. Evidence: HF-009 `10` minimum blockers and `12` gap. | ownership and reconciliation; one-owner invariant cannot be checked. | Define controlled owner references and explicit transfer/delegation records. | Validation rejects unknown, duplicate, or ambiguous owner references. |
| F-004 Major | Deterministic generation/migration remains specified but unimplemented. Evidence: HF-007 `09` implementation out of scope; `12` says generator/migration outside scope. | generated artifacts, version transition; reproducibility is unproven. | Build manifest-pinned generator/migration fixtures before adoption. | identical input manifests generate identical digests; unsupported versions fail explicitly. |
| F-005 Major | Synchronization transport, checkpoints, idempotency, replay, and discrepancy lifecycle are unspecified. Evidence: HF-009 `12`. | EOS/EENS/Zeus projections; direction/recovery cannot be qualified. | Define those contracts while retaining source-to-target rule. | Inject delivery/replay/version mismatch failures and show target rebuild without source mutation. |
| F-006 Major | Qualification and traceability have no executable evidence format/criteria. Evidence: HF-009 `10` says executable rules, fixtures, evidence format, and policy adoption are needed. | publication/adoption and gate verification; deterministic claims lack independent proof. | Encode qualification rules and retained evidence receipts. | Independent run validates lifecycle, ownership, graph, artifacts, and failures from fixtures. |
| F-007 Moderate | Existing artifacts lack automated provenance. Evidence: HF-007 `12` says manual artifacts do not claim automated provenance. | catalog/matrices/guides; a reader could confuse planning output with generated proof. | Label transitional artifacts and migrate only through provenance-bearing generator output. | Each generated artifact includes input manifest, schema/generator version, digest, sync and qualification status. |

No finding recommends a change to OA gate ordering, lifecycle/mission semantics, or controlled documentation.
