# Temporary Backup Infrastructure Commit Reconstruction Plan

Date: 2026-07-16

1. Requalify the exact repository, EOS, administrator, PV, VG, LV, and
   filesystem boundary.
2. Reconstruct C01 by creating the 400 GiB linear LV, formatting only that LV
   as ext4, installing its UUID-based fstab entry, mounting with
   `nosuid,nodev`, applying ownership/mode, and creating the empty governed
   workspace. Validate remaining extents and update INF-0001/AST-000002.
3. Reconstruct C02 by repeating non-destructive Playhouse name, route,
   neighbor, port, SSH, and bounded service discovery; preserve the blocked
   findings and architecture gate without inventing unavailable storage facts.
4. Validate controlled documents, whitespace, repository integrity, EOS,
   checkpoints, resume, LVM, filesystem, fstab, mount policy, empty workspace,
   connectivity evidence, and scope.
5. Commit C01, then C02. Do not amend, tag, push, migrate data, modify external
   storage, or create a Playhouse platform commit while qualification is
   blocked.
