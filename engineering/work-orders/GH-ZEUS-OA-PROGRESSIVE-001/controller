#!/usr/bin/env python3
"""Invoke the Progressive OA successive-gate controller."""
from pathlib import Path
import json
import sys

ROOT = Path(__file__).resolve().parents[3]
sys.path.insert(0, str(ROOT))
from scripts.lib.emp.progressive_oa import controller  # noqa: E402

print(json.dumps(controller(ROOT), indent=2, sort_keys=True))
