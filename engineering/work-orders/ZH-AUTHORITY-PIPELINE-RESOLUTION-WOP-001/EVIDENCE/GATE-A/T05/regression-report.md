# T05 Regression Report

Affected runtime, compatibility, CLI-consumer, OA-01/OA-02, next-action,
GateApprovalService, and OA-03 Mission Contract suites passed:
114 tests, 0 failures.

Controlled-document validation passed:
2,647 checks, 0 failures.

Repository `scripts/verify.sh` completed with:

- passed groups: 20;
- warnings: 0;
- failures: 0; and
- final status: system fully compliant.

`git diff --check` passed.

The two interruption tests that patch persistence internals now patch the
foundational owner of those mechanics. Their assertions are unchanged:
interruption before persistence creates no acceptance, and interruption after
receipt persistence recovers exactly one receipt.

