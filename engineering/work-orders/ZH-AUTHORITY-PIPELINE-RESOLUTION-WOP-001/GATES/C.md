# Gate C — Contract and Schema Reconciliation

Version and validate REAC, six receipt types, package indices, temporary
authority input manifest, and EWI Initiation Decision Record. Test zero/one/
many discovery and all cross-type substitutions. Rollback removes consumers
of new versions; never rewrites an issued receipt.

