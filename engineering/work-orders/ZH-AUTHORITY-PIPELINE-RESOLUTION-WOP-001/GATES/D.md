# Gate D — Repository Consolidation

Enforce the canonical root and one-way projections. Manifest and compare the
external OA tree, redirect consumers, rebuild/validate EOS, and prepare
read-only quarantine. No deletion or reverse synchronization. Rollback restores
consumer configuration only if it remains fail-closed; never restores an
independent authority writer.

