# Gate F — Test Isolation and Qualification

Use temporary repositories, clocks, keys, runtime state, gates, and remotes.
Run the full positive/negative matrix without production effects. Exit only
when repeated runs are deterministic and independent of live OA state.
Rollback discards bounded test workspaces and preserves test reports.

