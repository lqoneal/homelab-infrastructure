# OA-14 Operator Report

OA-14 requires explicit operator acceptance after canonical verification.
The verified authorization request is bound to the repository, current
Mission Knowledge Model, qualified agent, and OA-14 mission. Acceptance is
not inferred by qualification; it is recorded only by the canonical
`zeus approve OA-14 --operator <operator>` lifecycle command.
