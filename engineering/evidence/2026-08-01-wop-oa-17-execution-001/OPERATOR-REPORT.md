# OA-17 Operator Report

When authorization is pending or invalid, Zeus must identify the governing
Mission Knowledge Model/EMM/Capability Registry authority, the missing lease or
receipt prerequisite, and the next operator action: validate the bound request,
obtain the authority lease, or stop and recover. Human-readable output remains
the default; machine projections require explicit verification or JSON modes.

