# OA-08 Mission Queue Report

Queue ordering is deterministic: controlled mission sequence, then mission
identifier. Before OA-08 completion the queue contained OA-08; after completion
it contains OA-09. Replay produces the same queue and selection.
