# Human/JSON Parity Report

The semantic regression test has no JSON aggregate runner; its authoritative aggregate is unittest exit status. The controlled-document validator's machine-readable semantic report remains unchanged and continues to report document criteria, while human output now labels the test-only expected findings. No contradictory JSON disposition exists.
