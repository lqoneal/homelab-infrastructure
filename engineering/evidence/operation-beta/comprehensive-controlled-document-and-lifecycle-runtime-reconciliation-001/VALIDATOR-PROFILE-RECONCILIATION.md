# Validator and Semantic-Profile Reconciliation

The default semantic aggregate now excludes generated package roadmaps and historical immutable WOP artifacts. This prevents stale historical material from being treated as current normative authority while preserving explicit path validation. A regression test covers the exclusion and current Canonical WOP profile registration.

The assurance property for EMP progress/deviation traceability was aligned to the current normalized recommendation/unsupported reasoning contract. This is a focused validator/property correction, not suppression of a real conflict.

Results: controlled validation PASS; semantic-all 3805/0; assurance PASS; conformance PASS; implementation coverage PASS; focused profile tests PASS.

