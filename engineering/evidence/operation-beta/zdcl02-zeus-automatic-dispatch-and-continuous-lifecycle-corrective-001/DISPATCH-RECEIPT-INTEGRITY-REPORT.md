# Dispatch Receipt Integrity Report

Dispatch receipts bind WOP ID, lifecycle instance, repository, selected agent,
assignment digest, and deterministic policy. Receipt identity is derived from
the canonical assignment and is replay-stable.
