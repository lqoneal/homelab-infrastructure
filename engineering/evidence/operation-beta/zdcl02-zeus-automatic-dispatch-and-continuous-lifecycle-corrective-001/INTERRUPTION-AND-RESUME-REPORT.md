# Interruption and Resume Report

Existing Stage 1 replay tests pass. The same instance ID and receipt-backed
store are retained; automatic selection is retried only at the persisted
dispatch boundary.
