# Legacy Compatibility

Legacy classification is explicit: existing Stage 1 package directories and historical admission-record inputs retain the compatibility route. The absence of a traceability sidecar and the presence of `--repository` are not legacy signals. Current source plus generic `--approval` fails closed instead of silently entering the legacy contract. Historical records remain immutable and were not migrated or rewritten.

