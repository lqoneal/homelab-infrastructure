# Blocker Resolution Engine

Verification, resolve, graph, and history operations all consume the same evidence-derived framework. Resolution never fabricates evidence or mutates runtime state. A blocker is eligible for automatic retirement only when its authoritative condition no longer exists and reevaluation produces a non-blocking state. Policy and governance approvals remain operator actions.
