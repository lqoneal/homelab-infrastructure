# Validation Report

Validation results:

* controlled-document validation: PASS, 2897 checks, 0 failures;
* semantic-all validation: PASS, 3805 checks, 0 failures;
* conformance validation: PASS;
* assurance validation: PASS;
* registry validation: PASS, 87 objects;
* `scripts/engctl validate homelab`: PASS, all stages and 4 tests OK;
* Zeus platform verification: PASS;
* Operation Beta verification: PASS;
* repository/EOS synchronization validation: PASS;
* `git diff --check` and `git diff --cached --check`: PASS;
* focused and Wave 1–3 regressions: PASS as listed in `TEST-RESULTS.md`.

The two unrelated failures recorded in `TEST-RESULTS.md` remain classified and
were not modified. No publication, push, EOS synchronization, provider
evaluation, dispatch, or execution is part of this corrective.
