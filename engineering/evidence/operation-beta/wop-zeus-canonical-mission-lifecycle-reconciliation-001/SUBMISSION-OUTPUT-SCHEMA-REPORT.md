# Submission Output Schema Report

Consumers use `receipts.admission.admission_id` and `instance_id`.
`canonical_mission_linkage` is persisted on Development records without
replacing any Stage 1 identity.
