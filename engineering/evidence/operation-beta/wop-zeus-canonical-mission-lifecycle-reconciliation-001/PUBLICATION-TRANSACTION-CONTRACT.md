# Publication Transaction Contract

Verify candidate → publish → synchronize EOS → validate platform → activate
contract → reconcile registry → bind package → verify exact-cardinality
discovery → emit activation receipt. Prepublication code cannot bypass gates.
