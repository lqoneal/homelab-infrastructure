# Platform Validation

Validation is read-only for EOS, provider, runtime, publication, and `main`. Candidate qualification requires lifecycle tests, Registry, controlled-document validation, Zeus platform verification, and `git diff --check`.
