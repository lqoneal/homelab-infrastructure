# Blocker Execution Engine

`zeus blocker execute` dispatches the canonical executor for one blocker or all active blockers. It reports verified transitions, reevaluation, and decision recomputation, and remains fail-closed when operator action is required.
