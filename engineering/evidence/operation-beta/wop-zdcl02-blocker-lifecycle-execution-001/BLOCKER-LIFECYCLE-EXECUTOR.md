# Blocker Lifecycle Executor

`scripts.lib.emp.blocker_lifecycle` is the sole transition executor. It accepts canonical blocker objects, verifies each edge, and never writes authoritative evidence or protected runtime state.
