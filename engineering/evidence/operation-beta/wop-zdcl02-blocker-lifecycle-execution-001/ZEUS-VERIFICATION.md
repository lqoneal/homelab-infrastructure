# Zeus Verification

Lifecycle surfaces are `zeus blocker execute`, `resolve`, `revalidate`, `retire`, and `history`, alongside `zeus blockers`, `qualification`, `publication`, `readiness`, and `verify`. They use the same canonical blocker seeds.
