# WOP Authoring Integration Report

`wop template` continues to emit the canonical repository path. Source review
commands resolve identity through the shared validator. Existing source
documents may use the deterministic `homelab` alias without manual opaque
runtime-value construction.
