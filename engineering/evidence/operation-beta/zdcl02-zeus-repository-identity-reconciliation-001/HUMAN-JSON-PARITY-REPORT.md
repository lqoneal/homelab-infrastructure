# Human/JSON Parity Report

Identity resolution is represented in the same validation metadata used by
human and JSON WOP review output. The staged source reports canonical
`/data/engineering/repositories/homelab` in JSON; human review reports PASS
with the same source identity and corrective semantics.
