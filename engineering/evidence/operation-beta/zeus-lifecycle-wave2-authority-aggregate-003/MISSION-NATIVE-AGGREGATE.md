# Mission-Native Aggregate

Command:

```bash
ZEUS_RUNTIME_ROOT=/tmp/zeus-submission-canonicalization-4lKCNq \
  scripts/zeus mission aggregate ZEUS-EXECUTION-LIFECYCLE-COMPLETION-01 --json
```

Observed result was `PASS` with:

```text
MISSION_ID=ZEUS-EXECUTION-LIFECYCLE-COMPLETION-01
WOP_ID=WOP-ZEUS-EXECUTION-LIFECYCLE-COMPLETION-001
LIFECYCLE_STATE=ADMISSION_REQUESTED
NEXT_AUTHORIZED_ACTION=EVALUATE_MISSION_ADMISSION
AUTHORITY_CLASSIFICATION=CANONICAL
CURRENT_EXECUTION_READINESS=NOT_AVAILABLE
PROVIDER=NOT_STARTED
PROVIDER_SESSION=NOT_STARTED
EXECUTION=NOT_STARTED
PROCESS=NOT_STARTED
MONITORING=NOT_STARTED
EVIDENCE=NOT_STARTED
HISTORICAL_SESSION_EXECUTION_LEAK=NONE
READ_ONLY=true
REPLAY=IDEMPOTENT
```

The aggregate is a projection over the canonical resolver. It does not admit,
dispatch, invoke a provider, create a session, start execution, or mutate the
runtime.
