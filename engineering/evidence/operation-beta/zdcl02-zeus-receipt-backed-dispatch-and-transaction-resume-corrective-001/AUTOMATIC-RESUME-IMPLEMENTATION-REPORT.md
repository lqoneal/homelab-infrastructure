# Automatic Resume Implementation

The shared Stage 1 resolver is used by repeat submission and `zeus resume`. Resume does not create a package or registration and invokes only the next unmet transition. Invalid dispatch evidence blocks or rolls back deterministically.
