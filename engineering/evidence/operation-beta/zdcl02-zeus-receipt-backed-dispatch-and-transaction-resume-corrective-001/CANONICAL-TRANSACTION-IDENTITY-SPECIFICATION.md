# Canonical Transaction Identity

One Stage 1 record is authoritative. Resolution accepts instance ID, mission ID, WOP ID, package digest, source digest, or registration ID and rejects zero or multiple matches. No replacement registration or parallel transaction is created.
