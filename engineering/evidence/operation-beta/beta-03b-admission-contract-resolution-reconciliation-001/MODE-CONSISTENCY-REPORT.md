# Qualification and Operational Mode Consistency Report

Both modes invoke the same published contract/WOP resolver. They bind the
same mission, WOP, package, manifest, authority, repository, baseline, scope,
and submission. Qualification ends `QUALIFICATION_ONLY` with dispatch denied;
operational mode is the only mode that can expose dispatch permission, subject
to its approval boundary. No mode creates or replaces a WOP.
