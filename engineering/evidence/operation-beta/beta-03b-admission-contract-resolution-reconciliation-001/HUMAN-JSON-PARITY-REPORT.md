# Human/JSON Parity Report

`zeus admit-mission ... --json` emits the resolved admission object. The
`--human` projection formats that same object and reports result, admission ID,
decision, mission, WOP, authority, approval, submission, dispatch permission,
blockers, and next action. No second state calculation or authority source is
used.
