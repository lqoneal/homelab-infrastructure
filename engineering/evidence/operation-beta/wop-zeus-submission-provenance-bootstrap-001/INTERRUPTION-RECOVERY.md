# Interruption Recovery

The existing canonical resume path remains the recovery owner. The persisted bootstrap journal records completed derivation steps and the next projection step. Resume verifies immutable digests, preserves identities, repairs derived projections, and emits the existing reconciliation receipt.
