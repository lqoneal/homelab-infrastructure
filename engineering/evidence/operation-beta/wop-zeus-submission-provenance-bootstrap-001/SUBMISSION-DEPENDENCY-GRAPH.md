# Submission Dependency Graph

`source WOP -> validation -> source digest -> transaction -> submission receipt -> provenance -> authority snapshot -> package -> registration -> admission -> execution identity -> runtime projections`. Publication authority and EOS synchronization are downstream gates and are not inputs.
