# Admission and Execution Identity

Admission remains the existing receipt-backed Stage-1 admission projection. The bootstrap envelope binds its deterministic admission ID to the execution transaction and the existing execution identity, preserving compatibility with Stage-1 and runtime reconciliation controllers.
