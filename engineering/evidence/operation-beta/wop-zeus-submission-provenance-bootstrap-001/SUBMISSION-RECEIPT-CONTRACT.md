# Submission Receipt Contract

`submission_bootstrap.py` creates an immutable schema-versioned submission receipt with transaction, WOP, mission, source locator/digest, repository, branch, baseline, operator, mode, effect profile, bootstrap classification, authority source, receipt ID, and receipt digest.
