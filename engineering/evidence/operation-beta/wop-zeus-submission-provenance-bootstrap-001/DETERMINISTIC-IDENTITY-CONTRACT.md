# Deterministic Identity Contract

UUID5 identities are derived for submission transaction, submission receipt, provenance, execution transaction, registration/package binding, admission, authority snapshot, and execution. Identical source/context replays the same chain; changed source creates a new digest and cannot replay the prior active transaction.
