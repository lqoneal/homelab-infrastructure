# Runtime Initialization Report

Runtime admission and execution projections are derived after immutable chain creation by the existing atomic reconciler. Missing projections are repaired from the transaction; stale or digest-invalid authoritative records fail closed. EOS remains untouched on the candidate branch.
