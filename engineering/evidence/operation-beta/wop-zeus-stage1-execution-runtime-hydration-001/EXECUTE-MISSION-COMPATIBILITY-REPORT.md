# Execute-Mission Compatibility Report

`execute-mission` attempts canonical Stage 1 resolution before legacy
projection lookup. `start` can consume the receipt-backed admission identity;
`session` and `evidence` report the resolved transaction when execution is not
materialized; existing execution IDs are reused for resume.

The named live transaction was absent from this checkout, so the four command
forms were not run against it. Disposable resolver qualification passed;
full post-publication command qualification remains pending.
