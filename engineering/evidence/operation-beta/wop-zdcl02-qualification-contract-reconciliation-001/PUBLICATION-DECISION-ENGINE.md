# Publication Decision Engine

`qualification_contract.resolve()` is the single publication decision engine. Presentation commands call `view()` and cannot calculate readiness locally. Publication authority is a separate approval boundary and is never inferred from execution, bootstrap, or qualification authority.

