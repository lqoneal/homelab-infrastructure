# Qualification State Model

`QUALIFIED_FOR_PUBLICATION` is emitted only when required evidence is complete and no blocking record remains. Every missing, stale, contradictory, or non-definitive result emits `NOT_QUALIFIED`.

