# Regression Results

Canonical command smoke tests pass for schema and state shape. Existing bootstrap focused tests, platform verification, and `git diff --check` remain the required regression profile. The decision remains fail-closed with typed blockers.

