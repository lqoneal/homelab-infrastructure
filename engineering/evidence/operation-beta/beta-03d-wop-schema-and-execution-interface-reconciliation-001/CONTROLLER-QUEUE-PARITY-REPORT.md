# Controller and Queue Parity Report

`mission explain ZDCL-01`, `mission queue`, and `next-action` now project the
same existing execution ID, admission ID, gate, waiting category, current WOP
validation result, and resume command. The queue remains read-only and
operation-wide. Human-readable output is rendered from the same object as JSON.
