# Human/JSON Parity Report

The same controller result object is passed to the existing presentation
renderer. Machine output exposes `recommended_mission: CAGF-01` and the exact
next action. Human output exposes the same recommendation and next action.

The focused parity test verifies `mission recommend` in both modes and checks
all selection commands for the shared selection source
`operational_beta._selected_card`.
