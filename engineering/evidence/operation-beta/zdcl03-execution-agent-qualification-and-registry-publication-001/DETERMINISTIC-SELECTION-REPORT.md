# Deterministic Selection Report

`development_dispatch.select_provider` filters active, qualified, repository-scoped agents and sorts by `agent_id`. With the published candidate registry, selection returns `zeus-local-loneal-01`. Empty, revoked, unavailable, or foreign agents fail closed.
