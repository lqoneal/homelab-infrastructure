# Execution Agent Inventory

The existing qualification subsystem identified one current candidate: `zeus-local-loneal-01` (`local-authenticated`). It is repository-scoped to `/data/engineering/repositories/homelab`; no other active qualified agents are published.
