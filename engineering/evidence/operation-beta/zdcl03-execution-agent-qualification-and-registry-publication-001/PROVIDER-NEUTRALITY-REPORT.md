# Provider Neutrality Report

No provider is hard-coded. Selection consumes the existing registry, qualification status, capability declarations, repository scope, and deterministic ordering. Codex is not required; future qualified providers can be added through the same registry contract.
