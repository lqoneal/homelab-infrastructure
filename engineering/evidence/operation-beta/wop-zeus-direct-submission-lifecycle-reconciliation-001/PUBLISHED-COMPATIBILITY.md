# Published Compatibility

Published Mission Contract and package bindings remain supported through the authoritative admission resolver. The compatibility field `published_package_reused` remains present while the canonical semantic field is `authoritative_package_reused`; no published mission, package, or EOS state is modified.
