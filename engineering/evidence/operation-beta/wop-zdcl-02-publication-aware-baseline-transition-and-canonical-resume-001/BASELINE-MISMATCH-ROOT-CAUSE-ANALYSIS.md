# Baseline Mismatch Root Cause Analysis

The prior resume path compared current HEAD directly with the immutable submission baseline. Publication-aware classification now runs first and preserves both identities.
