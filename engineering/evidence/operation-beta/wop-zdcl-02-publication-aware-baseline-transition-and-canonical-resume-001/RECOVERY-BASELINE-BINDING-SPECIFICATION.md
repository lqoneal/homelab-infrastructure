# Recovery Baseline Binding Specification

The recovery binding is appended separately from `submission_baseline` and includes the verified transition digest, receipt identity, ancestry, repository identity, and validation state.
