# Negative Qualification

Disposable checks passed for each missing canonical field, invalid approval state, missing authority/reference values, incomplete sections, malformed DOCX, conflicting metadata, and package-generation failure. All fail before mutation. The protected package path is never rewritten during reconciliation.
