# Template and Init Report

`zeus wop init` and `zeus wop template` now use the same schema-driven metadata and emit all canonical fields. Markdown and DOCX extraction returned equivalent 31-field sets in disposable qualification. `--output` wrote exactly the requested Markdown and DOCX paths. No runtime or repository state was created by authoring commands.
