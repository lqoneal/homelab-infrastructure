# Receipt Chain Verification

Recovery verifies digest integrity and required order for validation, packaging, registration, authorization, and admission. Dispatch additionally requires a valid provider-selection receipt, matching transaction/provider/agent bindings, and a valid dispatch digest. Missing or malformed receipts fail closed.
