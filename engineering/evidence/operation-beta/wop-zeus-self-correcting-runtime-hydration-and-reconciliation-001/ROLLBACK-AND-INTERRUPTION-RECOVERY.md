# Rollback and Interruption Recovery

Injected failure at the promotion boundary restored existing files byte-for-byte and removed new files. Temporary files are cleaned; a later invocation re-discovers state.
