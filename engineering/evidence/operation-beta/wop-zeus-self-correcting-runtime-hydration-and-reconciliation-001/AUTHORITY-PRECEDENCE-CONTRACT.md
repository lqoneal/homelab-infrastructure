# Authority Precedence Contract

Stage 1 transaction and receipts precede admission, successor lineage, execution projection, and native session. Lower-precedence records cannot override conflicting immutable state; divergence fails closed.
