# Duplication Prevention

The Stage 1 transaction ID is derived once from mission, WOP, and package identity. Admission ID and execution ID are receipt/instance-bound. Reconciliation discovers by semantic identity, rejects non-equivalent duplicates, and creates no provider, dispatch, session, receipt, admission, execution, or transaction replacement.
