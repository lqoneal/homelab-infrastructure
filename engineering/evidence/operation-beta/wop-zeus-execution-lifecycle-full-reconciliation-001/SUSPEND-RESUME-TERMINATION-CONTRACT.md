# Suspend Resume Termination Contract

Suspend, resume, cancel, stop, and termination resolve the same transaction and execution identity. They are idempotent, preserve prior receipts, and fail closed for missing sessions, identity mismatch, corrupted state, or non-atomic persistence.
