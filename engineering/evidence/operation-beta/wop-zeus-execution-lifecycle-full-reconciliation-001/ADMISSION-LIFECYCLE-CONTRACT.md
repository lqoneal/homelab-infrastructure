# Admission Lifecycle Contract

Admission is complete only when its receipt-backed ID has a sealed projection matching the Stage 1 transaction, WOP, package/source digests, authority snapshot, repository baseline, and provider requirements. Missing or partial projections are derived from Stage 1; conflicting values fail closed.
