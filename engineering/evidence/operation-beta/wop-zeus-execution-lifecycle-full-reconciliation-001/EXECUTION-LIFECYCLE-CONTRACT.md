# Execution Lifecycle Contract

Execution identity is the Stage 1 `instance_id`. Execution is operationally established only when its projection exists, validates its state digest, binds the canonical admission and dispatch receipt, and is discoverable by status and session commands.
