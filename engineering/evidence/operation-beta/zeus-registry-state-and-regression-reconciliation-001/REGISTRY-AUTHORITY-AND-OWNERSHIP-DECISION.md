# Registry Authority and Ownership Decision

The capability YAML is the current capability authority; the execution-agent JSON is the controlled agent-registry candidate; mission knowledge and the EMM-resolved current WOP own current OA lifecycle projection. Runtime files and tests are derived or historical. Registry digests are validated from canonical JSON serialization with the digest field excluded. No cache or fixture supersedes these sources.

Decision: RECONCILED. No source conflict was found after separating live invariant tests from the frozen OA-05 fixture.
