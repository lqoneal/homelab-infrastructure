# Current-State Invariant Specification

Live tests require schema validation, unique capability IDs, resolvable internal dependencies, a valid authoritative active-gate projection, digest-valid execution-agent registry, unique agent IDs, and at least one qualified agent. They do not hard-code current counts or historical gate numbers.
