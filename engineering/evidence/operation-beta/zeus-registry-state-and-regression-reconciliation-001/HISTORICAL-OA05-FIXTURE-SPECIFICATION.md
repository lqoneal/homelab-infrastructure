# Historical OA-05 Fixture Specification

`scripts/tests/fixtures/oa05-capability-state.json` freezes the OA-05 qualification boundary (`OA-06`, five capabilities) for historical assertions. It is explicitly not loaded by live registry services and is not an active registry or work order.
