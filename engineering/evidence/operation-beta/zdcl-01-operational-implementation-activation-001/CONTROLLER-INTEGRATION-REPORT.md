# Controller Integration Report

Execution status carries `session_id`; canonical Beta execution detail carries `current_session`, lifecycle state, gate, effect profile, blockers, progress, and one next action. `execute-mission session`, `evidence`, and `handlers` expose session state, append-only history, and registry. Terminal sessions are historical.
