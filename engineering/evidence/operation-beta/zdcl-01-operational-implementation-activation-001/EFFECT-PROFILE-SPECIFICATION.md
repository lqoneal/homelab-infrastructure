# Effect Profile Specification

Profile `ZDCL-01-NATIVE-SESSION-FOUNDATION` permits runtime mutations beneath `native-sessions` and `mission-executions`, and create-only session evidence. Repository content, arbitrary filesystem, network, Production, and external publication effects are undeclared and rejected. Dry-run is side-effect free.
