# Canonical Fixture Factory

Positive fixtures now derive from `template_metadata`, `markdown_template`,
and `docx_template`. This guarantees approval state, authoritative references,
execution-package authority references, and all required sections are present
without production defaults or compatibility fallbacks.
