# Markdown/DOCX Parity

Canonical Markdown and DOCX sources are produced from the same metadata factory
and passed through the same extractor. The authoring parity suite passed,
including equivalent canonical field sets and exact output-path behavior.
