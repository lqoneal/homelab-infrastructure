# Execution State Machine

The controller defines the requested source-to-closeout states and maps receipt keys to lifecycle states. It never advances beyond an approval-gated publication state without an explicit policy approval.
