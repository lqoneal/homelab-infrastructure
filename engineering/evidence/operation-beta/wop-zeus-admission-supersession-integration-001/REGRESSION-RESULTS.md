# Regression Results

PASS: admission-supersession disposable tests (4), Stage 1 resolution tests (5), Zeus Stage 1 runtime tests (7), mission execution runtime tests (8), Registry validation, and `git diff --check`. The full platform result and unpublished-candidate classification are recorded separately.
