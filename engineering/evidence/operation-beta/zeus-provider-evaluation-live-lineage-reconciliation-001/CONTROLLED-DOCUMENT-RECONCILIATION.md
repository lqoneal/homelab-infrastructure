# Controlled-Document Reconciliation

Current documentation now distinguishes immutable provider-selection
provenance from the live current repository baseline. It states that ordinary
descendant publications preserve provider selection, while non-descendant
history, identity mismatch, digest mismatch, provider-registry ambiguity, and
ambiguous current artifacts fail closed.

It also states that provider identity is derived from the live execution-agent
registry and that dispatch-time availability/qualification is a later
revalidation boundary.

Structural controlled-document validation: PASS, 2,897 checks, 0 failures.
Semantic validation: PASS, 3,805 checks, 0 failures.

