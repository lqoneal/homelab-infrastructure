# Genuine Drift Negative Qualification

Disposable coverage rejects dirty trees, local/remote candidate divergence, and stale EOS. Published `main` with a mismatching EOS baseline is classified as `PUBLISHED_DRIFT`; an unpublished candidate with stale EOS is `EOS_STALE`. None is converted into a pass.
