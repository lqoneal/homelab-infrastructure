# Provider Launch Contract

An adapter must return `acknowledged: true`, `process_id`, `process_group_id`, and `health_digest`. The adapter cannot change authority, transaction, package, source, admission, or dispatch identities.
