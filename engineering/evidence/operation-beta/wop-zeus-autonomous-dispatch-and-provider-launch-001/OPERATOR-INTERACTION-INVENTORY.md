# Operator Interaction Inventory

Removed from the intended post-submission path: manual dispatch, `execute-mission start`, `execute-mission resume`, manual session creation, provider-specific launch, and runtime hydration. Governance approvals remain separate policy boundaries. The prepublication candidate performs no live launch.
