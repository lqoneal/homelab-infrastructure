# Codex Interruption Recovery

The atomic journal preserves the execution and session identities before launch.
A stopped, failed, or interrupted record can resume with the same context and
branch after validation. A running record replays without a second process.
