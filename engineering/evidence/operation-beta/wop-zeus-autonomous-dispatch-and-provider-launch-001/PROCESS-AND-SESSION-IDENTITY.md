# Process and Session Identity

Provider process ID, process-group ID, health digest, and deterministic session ID are derived runtime bindings. They are not substitutes for the Stage 1 execution identity and are rejected when transaction ownership is ambiguous.
