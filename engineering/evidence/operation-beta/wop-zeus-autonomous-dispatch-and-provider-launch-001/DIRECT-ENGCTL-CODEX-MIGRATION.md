# Direct engctl Codex Migration

Governed execution migrates from `engctl codex` to `zeus submit <authorized-wop>`.
The former command remains low-level compatibility infrastructure. Zeus-specific
provider and execution commands expose machine-backed state without requiring a
Codex terminal. No direct invocation is accepted as governed lifecycle evidence.
