# Prepublication Candidate Profile

A candidate is valid only when its clean `prepublication/*` branch is remote-aligned, descends from current published `main`, and EOS/checkpoint state represents current published `main`. The candidate remains intentionally unpublished; it is never synchronized into EOS by this validation path.
