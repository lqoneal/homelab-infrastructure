# Canonical Transaction Resume Report

The isolated resolver maps instance ID, mission ID, WOP ID, package digest, source digest, and registration ID to one transaction. Resume uses that identity and the next receipt-backed transition.
