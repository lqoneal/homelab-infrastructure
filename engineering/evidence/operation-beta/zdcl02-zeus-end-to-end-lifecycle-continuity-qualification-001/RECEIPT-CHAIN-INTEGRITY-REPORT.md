# Receipt Chain Integrity Report

Dispatch receipts require transaction, package, provider, agent, qualification, registry, plan, digest, and authority-snapshot fields. Invalid or replayed dispatch evidence cannot support `DISPATCHED`.
