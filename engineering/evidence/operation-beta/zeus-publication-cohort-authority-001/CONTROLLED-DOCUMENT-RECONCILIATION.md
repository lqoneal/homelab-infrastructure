# Controlled Document Reconciliation

The current canonical publication procedure now defines source-level cohort
authority, same-cohort shared-path semantics, immutable predecessor
supersession, live projection binding, and fail-closed stale/ambiguous input.
