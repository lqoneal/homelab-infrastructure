# Native Verification

The final identifiers and digests are recorded by the native completion report
and independently reproduced by `publication inspect`, `classify`, `prepare`,
`status`, and mission `snapshot` JSON projections. All are required to agree
before staging is authorized.
