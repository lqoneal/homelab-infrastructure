# Transaction Supersession

The original transaction is preserved byte-for-byte as historical evidence.
The replacement binds `publication_cohort_id` and
`supersedes_publication_id`; mission lookup excludes the predecessor through
successor linkage without rewriting predecessor candidate data or milestone
receipts.
