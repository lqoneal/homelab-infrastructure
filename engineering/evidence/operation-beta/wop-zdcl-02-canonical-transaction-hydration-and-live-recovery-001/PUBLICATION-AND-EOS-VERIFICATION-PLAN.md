# Publication and EOS Verification Plan

After authorized publication: verify clean `main == origin/main`, run `scripts/engctl eos synchronize homelab`, run `scripts/engctl platform validate homelab`, and require all stages to pass before live transaction recovery. No such mutation was performed here.
