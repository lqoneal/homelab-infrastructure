# Multigeneration Supersession Contract

Zeus follows every `superseded_by` link, requires the successor's `supersedes` link to point back to the predecessor, validates stable transaction and immutable digest bindings, and identifies exactly one terminal. A stale terminal may create one successor only after the governed transition policy passes.

