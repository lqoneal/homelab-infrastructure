# Publication Candidate Manifest

Publication was not performed. No files were staged or committed.

Bounded files created or modified by this corrective:

- `scripts/lib/emp/repository_projection.py` (new)
- `scripts/tests/test-zeus-repository-projection.py` (new)
- `scripts/zeus` (existing dirty file; bounded CLI/platform/doctor wiring)
- `scripts/lib/emp/next_action.py` (existing dirty file; bounded live-projection
  wiring for the compatibility fallback)
- `scripts/lib/emp/mission_verification_controller.py` (existing dirty file;
  bounded repository projection consumer wiring)
- `engineering/docs/architecture/ZEUS-MISSION-PROJECTION-SPECIFICATION.md`
  (existing dirty file; Git contract section)
- `engineering/docs/operations/ZEUS-GIT-PUBLICATION-BOUNDARY-PROCEDURE.md`
  (existing clean file; Git contract section)
- all files in this evidence directory.

All other pre-existing modified and untracked files remain unrelated and were
preserved. Candidate isolation is required before any later publication.
