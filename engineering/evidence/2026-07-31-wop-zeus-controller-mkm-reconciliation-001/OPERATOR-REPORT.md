# Operator Report

Expected active view:

```text
current_mission: OA-15
current_lifecycle: CURRENT
current_classification: BLOCKED
missing_capabilities: [ZEUS-OA-CAP-014]
next_action: Execute WOP-OA-15-EXECUTION-001
```

Default controller output remains human-readable. `--verify` emits sorted,
deterministic JSON and `--json` emits structured JSON. The reconciliation does
not authorize or execute the WOP.
