# PMCT Artifact Manifest

The controlled package contains:

- contract, operator guide, README, work-package procedure;
- locked YAML capability matrix and generator;
- repository-native Bash operator entry points;
- 30 auditable gate procedures;
- Bash helper libraries and Python authoritative-state-observation runtime;
- three JSON schemas and three evidence templates;
- controlled initial capability-state record;
- seven framework self-test modules and test runner;
- honest committed OA-01 example evidence;
- controlled-document reconciliation and Completion Report.

Integrity is validated by `git diff --check`, Git object integrity, PMCT
self-tests, schema checks, shell syntax checks, and each evidence package's
`artifacts.sha256` plus `COMPLETE` marker.
