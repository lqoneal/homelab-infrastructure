#!/usr/bin/env bash
# Repository-native Zeus Progressive Manual Capability Test interface.
set -u
set -o pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
exec python3 "${SCRIPT_DIR}/../lib/pmct.py" "$@"
